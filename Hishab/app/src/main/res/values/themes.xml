<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Hishab" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_700</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/purple_700</item>
        <item name="colorSecondaryVariant">@color/purple_700</item>
        <item name="colorOnSecondary">@color/purple_700</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/topViewColor</item>

        <!-- Customize your theme here. -->
        <!-- my custom colors. -->
        <item name="colorBlackWhite">@color/black</item>
        <item name="bgColor">@color/white</item>
        <item name="topViewColor">@color/purple_700</item>
        <item name="colorWhiteLightBlack">@color/white</item>
        <item name="filterButton">@color/white</item>
        <item name="filterText">@color/purple_700</item>

    </style>

</resources>